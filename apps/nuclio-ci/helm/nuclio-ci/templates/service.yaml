apiVersion: v1
kind: Service
metadata:
  name: {{ template "nuclio-ci.gatekeeper" . }}
  labels:
    app: {{ template "nuclio-ci.gatekeeper" . }}
    chart: {{ template "nuclio-ci.gatekeeper" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  ports:
    - port: 12345
      targetPort: http
      protocol: TCP
      name: ngrok

