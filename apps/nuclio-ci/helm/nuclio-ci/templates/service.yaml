apiVersion: v1
kind: Service
metadata:
  name: {{ template "nuclio-ci.fullname" . }}
  labels:
    app: {{ template "nuclio-ci.name" . }}
    chart: {{ template "nuclio-ci.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "nuclio-ci.name" . }}
    release: {{ .Release.Name }}
